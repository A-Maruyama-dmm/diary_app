<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-12'>
<h1>日記</h1>

  <table>
    <thead>
      <tr>
        <th>タイトル</th>
        <th>内容</th>
        <th colspan="3"></th>
      </tr>
   </thead>
   <tbody>
  <% @posts.each do |post| %>
     <tr>
       <td><%= post.title %></td>
       <td><%= post.content %></td>
       <td><%= link_to "Show", posts_path(post.id) %></td>
       <td><%= link_to "Edit", edit_booke_path(book.id) %></td>
       <td><%= link_to "Destroy", booke_path(book.id), method: :delete %></td>
     </tr>
  <% end %>
  </tbody>
  </table>

  </div>
 </div>
</div>


